//Sensor Model source
#include <sensor_model.h>

namespace sensor_model {

	//Lidar::Lidar(map::Map* map, int num_rays, double max_range, int n_bins, double min_angle, double max_angle) {
		//map_ = map;
	//}
}
